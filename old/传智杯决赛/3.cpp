#include <iostream>
#include <vector>

using namespace std;

int main()
{
    // freopen("./2.in","r",stdin);
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);

    int count,days;
    vector<int> field;
    cin >> count >> days;
    for (int i = 0; i < count; i++)
    {
        field.clear();
        int a;
        cin >> a;
        field.push_back(a);

                
    }

    int cnt=0;
    for (int i = 0; i < days-1; i++)
    {
        /* code */
    }
    
    
}